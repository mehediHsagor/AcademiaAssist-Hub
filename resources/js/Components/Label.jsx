import React from "react";

const Label = ({ label }) => {
    return (
        <div>
            <h1 className="text-3xl text-gray-600 mb-3">{label}</h1>
        </div>
    );
};

export default Label;
