const Title = ({ Title }) => {
    return (
        <div>
            <h1 className="text-4xl text-center my-4  text-gray-600 font-bold ">
                {Title}
            </h1>
        </div>
    );
};

export default Title;
